const user = '/user';
export const userApi = {
  regist: `${user}/register`, // 注册
  login: `${user}/login`, // 登陆
  userList: `${user}/userList`, // 获取用户列表
  deleteUser: `${user}/deleteUser`, // 删除；
  updateUser: `${user}/updateUser`, // 更新
};
